export interface FQ {
    id: number
    category: string
    question: string
}

export interface FA {
    id: number
    answer: string
}

export interface FAQ {
    id: number
    category: string
    question: string
    answer: string
}